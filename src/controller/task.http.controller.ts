import {Router} from 'express';

const controller = Router();

controller.get("/", );
controller.post('/', );
controller.patch('/', );
controller.delete('/', );

export {controller as TaskHttpController};
